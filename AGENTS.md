# Dotfiles

## Codex Instructions

- Do not write tests unless the user explicitly asks for tests.
- If temporary tests are necessary to validate a change, use them only as transitive scaffolding during development.
- Remove any temporary tests before finishing the task unless the user explicitly asked for them to remain.
